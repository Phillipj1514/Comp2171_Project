import 'package:flutter/material.dart';
class ClientProgressReport extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
	    color: Colors.white30

    );
  }
}
